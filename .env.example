# Database Connection String
# For Vercel Postgres: postgresql://[user]:[password]@[host]:[port]/[db]?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/classguard"

# JWT Secret for authentication
JWT_SECRET="generate-a-long-random-string-here"

# Optional: Environment (production/development)
NODE_ENV="production"
